// Merge Sort: Counting Inversions

#include <algorithm>
#include <vector>

using namespace std;

long countInversion(vector<int> arr) {
	return 1;
}

int main() {
	vector<int> a{ 1,2,3,4,5 };
	countInversion(a);
	return 0;
}
